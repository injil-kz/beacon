import 'package:flutter/material.dart';

class BeaconSingleRequestScreen extends StatelessWidget {
  const BeaconSingleRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
