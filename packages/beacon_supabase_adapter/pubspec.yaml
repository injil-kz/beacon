name: beacon_supabase_adapter
description: Supabase Adapter for Beacon HTTP Logger.
version: 1.0.18
homepage: https://injil.kz

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  injil_beacon: ^1.0.18
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
